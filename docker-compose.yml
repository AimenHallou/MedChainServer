version: '3.9'
services:
    api:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - '80:8080'
        environment:
            - MONGO_URI=${MONGO_URI}
            - JWT_SECRET=${JWT_SECRET}
