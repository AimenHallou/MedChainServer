version: '3.9'
services:
    api:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - '443:4000'
        environment:
            - MONGO_URI=${MONGO_URI}
            - JWT_SECRET=${JWT_SECRET}
